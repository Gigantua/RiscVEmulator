/* arpa/inet.h — empty stub; net_main.c includes it but the bare-metal port
 * only uses the loopback driver (net_none + net_loop) so no real socket
 * API is exercised. */
#ifndef RVEMU_ARPA_INET_H
#define RVEMU_ARPA_INET_H
#endif
